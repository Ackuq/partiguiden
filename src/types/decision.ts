export interface Decision {
  id: number;
  paragraph: string;
  paragraphTitle: string;
  authority: string;
  denomination: string;
  title: string;
  voteSearchTerm: string;
  votesExists: boolean;
}
